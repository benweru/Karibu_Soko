<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Karibu_Soko"
        tools:targetApi="31">
        <activity
            android:name="com.example.activities.OnboardingActivity"
            android:exported="false" />
        <activity
            android:name="com.example.activities.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Karibu_Soko" />
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Add these activities for your SignInActivity and SignUpActivity -->
        <activity
            android:name="com.example.activities.SignInActivity"
            android:exported="true"
            android:label="@string/sign_in_activity_label"
            android:theme="@style/Theme.Karibu_Soko" />
        <activity
            android:name="com.example.activities.SignUpActivity"
            android:exported="true"
            android:label="@string/sign_up_activity_label"
            android:theme="@style/Theme.Karibu_Soko" />
        <activity
            android:name="com.example.activities.CartActivity"
            android:exported="true"
            android:label="Cart"
            android:theme="@style/Theme.Karibu_Soko" />
        <activity
            android:name="com.example.activities.CheckoutActivity"
            android:exported="true"
            android:label="Checkout"
            android:theme="@style/Theme.Karibu_Soko" />

    </application>

</manifest>